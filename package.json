{
  "name": "atomic-dom",
  "version": "0.1.0",
  "description": "ASXR language parser and compiler for atomic DOM operations",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/**/*.test.js",
    "dev": "tsc --watch"
  },
  "keywords": [
    "dom",
    "atomic",
    "parser",
    "compiler",
    "asxr"
  ],
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
