# K'UHUL Atomic Expert Training Requirements
# Install with: pip install -r requirements.txt

# Core ML
torch>=2.0.0
transformers>=4.36.0
accelerate>=0.25.0
peft>=0.7.0

# Data handling
datasets>=2.15.0
pyarrow>=14.0.0
huggingface_hub>=0.19.0

# Training utilities
tensorboard>=2.15.0
wandb>=0.16.0  # Optional: for experiment tracking

# Quantization
bitsandbytes>=0.41.0  # For 4-bit quantization
auto-gptq>=0.5.0  # Optional: GPTQ quantization

# Utilities
tqdm>=4.66.0
numpy>=1.24.0
scipy>=1.11.0

# Flash Attention (optional, for faster training)
# flash-attn>=2.3.0  # Requires CUDA compilation

# For data preprocessing
sentencepiece>=0.1.99
tiktoken>=0.5.0
